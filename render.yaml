services:
  - type: web
    name: standup-pro
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disks:
      - name: standup-data
        mountPath: /opt/render/project/src
        sizeGB: 1
